name: Build and Release HydraideCTL

on:
  push:
    tags:
      - "hydraidectl/v*"

jobs:
  build-cli:
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read

    strategy:
      matrix:
        include:
          # Linux AMD64 build
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            binary_name: hydraidectl-linux-amd64
            asset_name: hydraidectl-linux-amd64

          # Linux ARM64 build
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
            binary_name: hydraidectl-linux-arm64
            asset_name: hydraidectl-linux-arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Extract version from tag
        id: extract_version
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
          check-latest: true

      - name: Build binary for target platform
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          # Strip 'hydraidectl/' prefix from version (e.g., hydraidectl/v1.0.4 -> v1.0.4)
          VERSION=${{ steps.extract_version.outputs.version }}
          VERSION=${VERSION#hydraidectl/}
          COMMIT=${{ github.sha }}
          BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          
          go build -a -installsuffix cgo \
            -ldflags="-X 'github.com/hydraide/hydraide/app/hydraidectl/cmd.Version=${VERSION}' \
                      -X 'github.com/hydraide/hydraide/app/hydraidectl/cmd.Commit=${COMMIT}' \
                      -X 'github.com/hydraide/hydraide/app/hydraidectl/cmd.BuildDate=${BUILD_DATE}'" \
            -o ${{ matrix.binary_name }} ./app/hydraidectl

      - name: Upload built binary as artifact
        uses: actions/upload-artifact@v4
        with:
          name: hydraidectl-${{ matrix.goos }}-${{ matrix.goarch }}
          path: ${{ matrix.binary_name }}

  create_release:
    needs: build-cli
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Extract version from tag
        id: extract_version
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Download all built artifacts
        uses: actions/download-artifact@v5
        with:
          path: ./artifacts

      - name: Move binaries to root for release
        run: |
          mv ./artifacts/hydraidectl-linux-amd64/hydraidectl-linux-amd64 .
          mv ./artifacts/hydraidectl-linux-arm64/hydraidectl-linux-arm64 .

      - name: Create GitHub release with uploaded binaries
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ steps.extract_version.outputs.version }}
          name: HydraideCTL ${{ steps.extract_version.outputs.version }}
          artifacts: "hydraidectl-linux-amd64,hydraidectl-linux-arm64"
          generateReleaseNotes: true
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}
